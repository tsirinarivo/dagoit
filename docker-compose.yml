services:
  dagoit:
    build: .
    container_name: dagoit
    restart: unless-stopped
    ports:
      - "127.0.0.1:8085:3000"
    environment:
      - NODE_ENV=production
      - NEXT_PUBLIC_SITE_URL=https://dago-it.com
